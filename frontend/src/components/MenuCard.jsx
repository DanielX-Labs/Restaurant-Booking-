import { useContext } from "react";
import { ArrowUpRight, ShoppingBag } from "lucide-react";
import { AppContext } from "../context/AppContext";

const MenuCard = ({ menu }) => { const { navigate, addToCart } = useContext(AppContext); return <article className="group overflow-hidden rounded-[1.75rem] border border-slate-200/80 bg-white shadow-[0_8px_35px_rgba(15,23,42,.06)] transition duration-500 hover:-translate-y-2 hover:shadow-[0_24px_60px_rgba(15,23,42,.13)]"><button onClick={() => navigate(`/menu-details/${menu._id}`)} className="relative block h-60 w-full overflow-hidden text-left"><img src={menu.image} alt={menu.name} loading="lazy" className="size-full object-cover transition duration-700 group-hover:scale-105"/><span className="absolute inset-0 bg-gradient-to-t from-slate-950/65 via-transparent to-transparent"/><span className="absolute right-4 top-4 grid size-10 place-items-center rounded-full bg-white/90 text-slate-900 opacity-0 shadow-lg backdrop-blur transition group-hover:opacity-100"><ArrowUpRight size={18}/></span>{!menu.isAvailable && <span className="absolute left-4 top-4 rounded-full bg-slate-950/85 px-3 py-1.5 text-xs font-bold text-white">Currently unavailable</span>}</button><div className="p-5"><h3 className="truncate text-xl font-bold tracking-tight text-slate-950">{menu.name}</h3><p className="mt-2 line-clamp-2 min-h-11 text-sm leading-6 text-slate-500">{menu.description}</p><div className="mt-5 flex items-center justify-between gap-3"><p className="text-xl font-black text-slate-950">₦{Number(menu.price || 0).toLocaleString()}</p><button onClick={() => addToCart(menu._id)} disabled={!menu.isAvailable} className="inline-flex items-center gap-2 rounded-full bg-orange-500 px-4 py-2.5 text-sm font-bold text-white transition hover:bg-orange-600 active:scale-95 disabled:cursor-not-allowed disabled:bg-slate-200 disabled:text-slate-400"><ShoppingBag size={16}/>Add</button></div></div></article>; };
export default MenuCard;
