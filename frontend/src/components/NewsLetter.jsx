import { useContext, useState } from "react";
import { ArrowRight, Mail } from "lucide-react";
import { toast } from "sonner";
import { AppContext } from "../context/AppContext";
const NewsLetter = () => { const { axios } = useContext(AppContext); const [email,setEmail]=useState(""); const [loading,setLoading]=useState(false); const subscribe=async(event)=>{event.preventDefault();try{setLoading(true);const{data}=await axios.post("/api/engagement/subscribe",{email});if(data.success){toast.success(data.message);setEmail("");}else toast.error(data.message);}catch(error){toast.error(error.response?.data?.message||"Subscription failed");}finally{setLoading(false);}}; return <section className="public-container py-20"><div className="relative overflow-hidden rounded-[2.5rem] bg-slate-950 px-6 py-14 text-white shadow-2xl sm:px-12 lg:px-16"><div className="absolute -right-20 -top-24 size-80 rounded-full bg-orange-500/30 blur-3xl"/><div className="relative grid items-center gap-10 lg:grid-cols-2"><div><p className="text-sm font-bold uppercase tracking-[.2em] text-orange-400">Stay in the loop</p><h2 className="mt-3 text-3xl font-black tracking-tight sm:text-5xl">Fresh updates, served occasionally.</h2><p className="mt-4 max-w-lg leading-7 text-slate-400">Get restaurant news and menu updates delivered directly to your inbox.</p></div><form onSubmit={subscribe} className="rounded-2xl bg-white p-2 sm:flex"><label className="relative block flex-1"><Mail className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" size={19}/><input type="email" value={email} onChange={(event)=>setEmail(event.target.value)} required placeholder="Your email address" className="h-13 w-full rounded-xl pl-12 pr-4 text-slate-900 outline-none"/></label><button disabled={loading} className="mt-2 inline-flex h-13 w-full items-center justify-center gap-2 rounded-xl bg-orange-500 px-5 font-bold text-white hover:bg-orange-600 disabled:opacity-60 sm:mt-0 sm:w-auto">{loading?"Joining…":"Subscribe"}<ArrowRight size={17}/></button></form></div></div></section>; };
export default NewsLetter;
