import { useContext } from "react";
import { AppContext } from "../context/AppContext";
const Categories = () => { const { categories } = useContext(AppContext); if (!categories.length) return null; return <section className="bg-[#fffdf9] py-20"><div className="public-container"><div className="max-w-2xl"><p className="text-sm font-bold uppercase tracking-[.2em] text-orange-500">Find your favourite</p><h2 className="mt-3 text-4xl font-black tracking-tight sm:text-5xl">Explore by <span className="font-serif italic text-orange-500">category</span></h2><p className="mt-4 leading-7 text-slate-500">Discover delicious dishes from our carefully curated categories.</p></div><div className="mt-10 flex gap-5 overflow-x-auto pb-4 sm:grid sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5">{categories.map((category, index) => <article key={category._id} className="group min-w-36 motion-safe:animate-[riseIn_.6s_ease-out_both]" style={{animationDelay:`${index * 60}ms`}}><div className="aspect-square overflow-hidden rounded-[2rem] bg-slate-100 shadow-lg shadow-slate-900/5"><img src={category.image} alt={category.name} loading="lazy" className="size-full object-cover transition duration-700 group-hover:scale-110"/></div><h3 className="mt-4 text-center font-bold text-slate-800">{category.name}</h3></article>)}</div></div></section>; };
export default Categories;
